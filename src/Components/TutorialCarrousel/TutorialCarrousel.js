import React, { useState } from 'react'

export const TutorialCarrousel = () => {
    // USESTATES
    const [tutPage, setTutPage] = useState(0)

    // Variables
    let titulos = [
        '',
        '',
        '',
    ]

    return (
        <>
            {(tutPage === 0) && (
                <>
        
                </>
            )}
        </>
    )
}
